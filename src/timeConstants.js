// THESE ARE FOR PR REVIEW TESTING ONLY
const fakeTimeMonth = `05`;
const fakeTimeDay = `05`;
const fakeTimeHour = `02`;
const fakeTimeMinute = `51`;
const fakeTimeSeconds = `36.38`;
export const fakeCurrentTime = `2020-${fakeTimeMonth}-${fakeTimeDay}T${fakeTimeHour}:${fakeTimeMinute}:${fakeTimeSeconds}Z`;
